// Template String representation now

let output={
    item:"iphone xs",
    price:20000,
};
console.log("phone:",output.item,"price",output.price);
console.log(`item :${output.item} and price is ${output.price} taka`); //when use template str then number convert single ---single string-----